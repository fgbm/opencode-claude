// Entry for loading this checkout as a local OpenCode v2 plugin: for a plugin
// directory OpenCode looks for server.js or index.js, not package.json main.
export { default } from "./opencode-claude.js";
